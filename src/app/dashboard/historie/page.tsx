import { RevenueHistoryContainer } from '@/app/components/Revenue/RevenueHistoryContainer';

export const revalidate = 0;

export default async function Historie() {
	return <RevenueHistoryContainer />;
}
